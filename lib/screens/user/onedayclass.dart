import 'package:flutter/material.dart';

class OnedayClassScreen extends StatelessWidget {
  const OnedayClassScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('원데이 클래스')),
      body: Center(child: Text('원데이 클래스 페이지')),
    );
  }
}
