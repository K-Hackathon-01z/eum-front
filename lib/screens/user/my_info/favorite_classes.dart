import 'package:flutter/material.dart';

class FavoriteClasses extends StatelessWidget {
  const FavoriteClasses({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('나의 관심 등록 클래스'));
  }
}
