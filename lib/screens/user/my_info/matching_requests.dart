import 'package:flutter/material.dart';

class MatchingRequests extends StatelessWidget {
  const MatchingRequests({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('매칭 신청 내역'));
  }
}
